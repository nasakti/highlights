{
  "name": "highlights",
  "version": "1.0.0",
  "description": "Personal reading highlights collection — browse, search, and export highlights from Kindle and Apple Books",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vitest": "^3.0.0"
  }
}
